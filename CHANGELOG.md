# 变更记录

本项目遵循语义化版本。每个发布版本对应一个 Git 标签和可复现的构建制品。

## [未发布]

- 新服务默认支持“记住 API Key”，在本机持久保存，浏览器重启后无需重复填写或解锁。
- 保留会话和口令加密方式；旧配置只有显式保存时才转换。设置页说明未加密存储风险。
- local/session 存储限制为可信扩展上下文，增加密钥模式切换、重启后读取和删除的回归测试。

## [0.2.1] - 2026-07-27

### 修复

- 修复 Content Script 在打包产物中早于样式常量初始化，导致选区工具栏进入网页正文流的问题。
- 在严格 `style-src 'none'` Content Security Policy 页面保留工具栏和内联结果弹窗的关键布局。
- 修复安装、更新或授权前已打开标签页未自动注入工具栏的问题。
- 串行化右键菜单重建，避免重复菜单 ID 与父菜单缺失错误。

### 改进

- 工具栏动作默认在选区附近打开流式结果弹窗；右键菜单和扩展入口保留 Side Panel 工作区。
- 增加严格 CSP、全站注入和右键菜单并发的自动化回归测试。

### 已知限制

- Chrome 内部页面、Chrome 网上应用店和受限 PDF 页面不支持注入。
- 当前未发布 Chrome Web Store；用户需通过开发者模式加载已解压构建。

## [0.2.0] - 2026-07-27

### 新增

- 初始 Manifest V3 扩展：选区工具栏、右键菜单、Side Panel、OpenAI/Azure 兼容接口、自定义 Prompt、可选本地历史和本地加密 API Key。
